# File: k8s/local/pdf-generator/server.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: pdf-server
  namespace: platform-local
spec:
  selector:
    matchLabels:
      app: pdf-server
  template:
    metadata:
      labels:
        app: pdf-server
    spec:
      serviceAccountName: pdf-server
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
      - name: pdf-server
        image: pdf-server:local
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 4001
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
            - ALL
        resources:
          requests:
            cpu: 100m
            memory: 256Mi
            ephemeral-storage: 512Mi
          limits:
            cpu: 1
            memory: 1Gi
            ephemeral-storage: 2Gi
        readinessProbe:
          httpGet:
            path: /api/v1/pdf/health
            port: 4001
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /api/v1/pdf/health
            port: 4001
          initialDelaySeconds: 20
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: pdf-server
  namespace: platform-local
spec:
  selector:
    app: pdf-server
  ports:
  - port: 4001
    targetPort: 4001