# File: k8s/local/topology-service/client.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: topology-client
  namespace: platform-local
  labels:
    app: topology-client
spec:
  replicas: 1
  selector:
    matchLabels:
      app: topology-client
  template:
    metadata:
      labels:
        app: topology-client
    spec:
      serviceAccountName: topology-client
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: topology-client
          image: topology-client:local
          ports:
            - containerPort: 3011
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
              - ALL
          resources:
            requests:
              cpu: 10m
              memory: 128Mi
              ephemeral-storage: 128Mi
            limits:
              cpu: 250m
              memory: 512Mi
              ephemeral-storage: 512Mi
          readinessProbe:
            tcpSocket:
              port: 3011
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 3011
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: topology-client
  namespace: platform-local
spec:
  type: ClusterIP
  selector:
    app: topology-client
  ports:
    - port: 3011
      targetPort: 3011
      protocol: TCP