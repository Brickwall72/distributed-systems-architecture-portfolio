# Local topology service environment profile for the graph validation boundary.
# These values are intentionally kept in a service-local .env file so the compose manifest can
# remain portable and the repo can avoid a monolithic infrastructure config (ADR-006).
PORT=8081
DB_USER=neo4j
DB_PASSWORD=SecureAviationToken71
DB_EXTERNAL_PORT=7474
