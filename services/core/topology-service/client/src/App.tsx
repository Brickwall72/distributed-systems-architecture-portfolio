// File: services/core/topology-service/client/src/App.tsx

import TopologyCanvas from './TopologyCanvas';

/**
 * Root application view for the topology service client.
 *
 * This is the top-level component mounted by the client bootstrap and is
 * responsible for rendering the interactive topology canvas experience.
 */
export default function App() {
  return <TopologyCanvas />;
}
