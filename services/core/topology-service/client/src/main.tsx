// File: services/core/topology-service/client/src/main.tsx

/*
 * Entry point for the topology client bundle.
 * The lazy bootstrap keeps the application shell lean while allowing the
 * runtime to resolve the actual render logic after the browser has loaded.
 */
import('./bootstrap');
