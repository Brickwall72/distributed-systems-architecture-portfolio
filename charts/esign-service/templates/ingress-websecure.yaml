# File: charts/esign-service/templates/ingress-websecure.yaml
{{- if .Values.ingress.websecure.enabled }}
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: {{ .Release.Name }}-websecure
  labels:
    app.kubernetes.io/name: esign
    app.kubernetes.io/component: ingress
spec:
  entryPoints:
    {{- toYaml .Values.ingress.websecure.entryPoints | nindent 4 }}
  routes:
    - match: {{ .Values.ingress.api.rule | quote }}
      kind: Rule
      services:
        - name: {{ .Values.ingress.api.serviceName }}
          port: 80
      middlewares:
        - name: {{ .Release.Name }}-api-rewrite
        - name: gzip-compress
        - name: security-headers
    - match: {{ .Values.ingress.client.rule | quote }}
      kind: Rule
      services:
        - name: {{ .Values.ingress.client.serviceName }}
          port: 80
      middlewares:
        - name: gzip-compress
        - name: security-headers
  tls:
    {{- toYaml .Values.ingress.websecure.tls | nindent 4 }}
{{- end }}

